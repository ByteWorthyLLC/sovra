---
phase: 05-multi-agent
plan: 07
type: execute
wave: 4
depends_on: ["05-01", "05-06"]
files_modified:
  - supabase/migrations/20260412200000_phase5_workspace_agents.sql
autonomous: false
requirements:
  - MUL-01
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-05
  - REAL-01
  - REAL-02
  - REAL-03
  - REAL-04
  - REAL-05
  - MUL-02
  - MUL-03
  - MUL-04
  - MUL-05
  - MUL-06

must_haves:
  truths:
    - "Database schema includes workspace_agents table with RLS"
    - "All tests pass across both web and worker packages"
    - "TypeScript compiles without errors"
    - "Go worker compiles without errors"
    - "Workspace list page loads and displays workspaces"
  artifacts:
    - path: "supabase/migrations/20260412200000_phase5_workspace_agents.sql"
      provides: "Applied migration for workspace_agents, memory_strategy, sequential mode"
  key_links:
    - from: "supabase migration"
      to: "workspace CRUD actions"
      via: "workspace_agents table must exist for actions to work"
      pattern: "workspace_agents"
---

<objective>
Apply the database migration and run the full integration verification to confirm all Phase 5 components work together. This is the gate that confirms the migration is applied and everything compiles/tests clean.

Purpose: The migration created in Plan 01 must be pushed to the database before any workspace features can function end-to-end. This plan applies it and runs the full quality gate.
Output: Applied migration, full test suite green, human verification of workspace UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-agent/05-01-SUMMARY.md
@.planning/phases/05-multi-agent/05-06-SUMMARY.md

@supabase/migrations/20260412200000_phase5_workspace_agents.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply database migration + full test suite + build verification</name>
  <files>
    supabase/migrations/20260412200000_phase5_workspace_agents.sql
  </files>
  <read_first>
    supabase/migrations/20260412200000_phase5_workspace_agents.sql
  </read_first>
  <action>
    1. [BLOCKING] Apply the Phase 5 migration:
       ```bash
       cd /Users/kevinrichards/Projects/agentforge
       npx supabase db push
       ```
       If supabase db push requires interactive confirmation, try:
       ```bash
       echo "y" | npx supabase db push
       ```
       If that fails due to interactive prompts that cannot be suppressed, flag for manual intervention.

    2. Verify migration applied:
       ```bash
       npx supabase db diff --linked
       ```
       Should show no pending changes (migration fully applied).

    3. Run full TypeScript test suite:
       ```bash
       cd packages/web && pnpm test -- --reporter=verbose
       ```
       All tests in lib/workspace/__tests__/ and lib/memory/__tests__/ must pass.

    4. Run full Go test suite:
       ```bash
       cd packages/worker && go test ./... -v -count=1
       ```
       All tests in internal/socketio/ must pass.

    5. TypeScript compilation check:
       ```bash
       cd packages/web && npx tsc --noEmit
       ```

    6. Go build check:
       ```bash
       cd packages/worker && go build ./cmd/worker/
       ```

    7. Lint check:
       ```bash
       cd packages/web && pnpm lint 2>&1 | tail -5
       ```
  </action>
  <verify>
    <automated>cd /Users/kevinrichards/Projects/agentforge/packages/web && pnpm test -- --reporter=dot && npx tsc --noEmit && cd ../worker && go test ./... -count=1 && go build ./cmd/worker/</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exits 0 (migration applied)
    - All TypeScript tests pass (exit 0)
    - All Go tests pass (exit 0)
    - tsc --noEmit exits 0 (no type errors)
    - go build ./cmd/worker/ exits 0 (worker compiles)
  </acceptance_criteria>
  <done>Migration applied to Supabase. All tests green. TypeScript and Go both compile cleanly. Full quality gate passed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full Phase 5 workspace UI</name>
  <action>
    Human verifies the complete workspace UI by navigating the application and checking all components render correctly, real-time indicators work, and all copy follows the spec.
  </action>
  <what-built>
    Full Phase 5: Multi-Agent Workspaces with real-time collaboration.
    - Workspace CRUD (create, list, update, delete)
    - Agent assignment to workspaces
    - 5 collaboration modes (round_robin, parallel, sequential, hierarchical, democratic)
    - 4 memory strategies (conversation, summary, vector, hybrid)
    - Context compression (snip, microcompact, collapse)
    - Conflict resolution (vote, hierarchy, consensus)
    - Real-time Socket.IO server in Go worker
    - Socket.IO client with reconnection handling
    - Workspace detail page with activity feed, agent panel, shared memory panel
    - Settings sheet with General/Collaboration/Memory tabs
    - Premium dark-mode UI with animations
  </what-built>
  <how-to-verify>
    1. Start the development environment: `docker-compose up` from project root
    2. Navigate to any tenant: http://localhost:3000/t/{slug}/workspaces
    3. Verify sidebar shows "Workspaces" nav item between Agents and Members
    4. Verify empty state: "No workspaces yet" with "Create workspace" CTA
    5. Click "Create workspace":
       - Enter a name and description
       - Select a collaboration mode (try round_robin)
       - Select agents (if any exist)
       - Click "Create workspace"
    6. Verify workspace appears in the grid with correct mode badge
    7. Click into the workspace detail page:
       - Verify header shows workspace name + mode badge + socket status
       - Verify agent panel (right side) shows assigned agents
       - Verify shared memory panel (bottom) is collapsible
       - Click "Workspace settings" and verify 3 tabs: General, Collaboration, Memory
    8. Check socket status indicator:
       - Should show green "Live" dot if Go worker is running on port 3002
       - Should show red "Offline" if worker is not running
    9. Verify all text uses no em dashes (long dashes)
    10. Verify mobile responsive: resize browser to less than 768px, agent panel should collapse to sheet
  </how-to-verify>
  <verify>Human approves UI or reports issues</verify>
  <acceptance_criteria>
    - Workspace list page renders with grid layout
    - Create workspace dialog opens and submits successfully
    - Workspace detail page renders all panels (activity feed, agents, shared memory)
    - Socket status indicator reflects actual connection state
    - Settings sheet has 3 tabs with all options
    - No em dashes in any visible text
    - Mobile responsive layout works
  </acceptance_criteria>
  <done>Human approves the complete Phase 5 workspace UI</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration to database | Schema changes applied to live Supabase instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Tampering | migration rollback | accept | Supabase migrations are forward-only in this project. Rollback requires manual SQL. Acceptable for v1 boilerplate. |
</threat_model>

<verification>
1. Database migration applied successfully
2. All TypeScript and Go tests pass
3. Both packages compile cleanly
4. Human verifies UI renders correctly
</verification>

<success_criteria>
- Migration applied
- Full test suite green
- Human approves workspace UI functionality
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-agent/05-07-SUMMARY.md`
</output>
