---
phase: 05-multi-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260412200000_phase5_workspace_agents.sql
  - packages/web/lib/workspace/types.ts
  - packages/web/lib/workspace/actions.ts
  - packages/web/lib/workspace/queries.ts
  - packages/web/lib/workspace/shared-memory.ts
  - packages/web/lib/workspace/__tests__/actions.test.ts
  - packages/web/lib/workspace/__tests__/queries.test.ts
  - packages/web/lib/workspace/__tests__/shared-memory.test.ts
autonomous: true
requirements:
  - MUL-01
  - MUL-03

must_haves:
  truths:
    - "workspace_agents join table exists with RLS policies"
    - "Workspace CRUD actions create, list, update, delete workspaces"
    - "Agents can be assigned to and removed from workspaces"
    - "Shared memory can be read, written, and upserted per workspace"
    - "All queries are tenant-scoped via Supabase RLS"
  artifacts:
    - path: "supabase/migrations/20260412200000_phase5_workspace_agents.sql"
      provides: "workspace_agents join table, memory_strategy column, sequential mode"
      contains: "create table workspace_agents"
    - path: "packages/web/lib/workspace/types.ts"
      provides: "Workspace, WorkspaceAgent, SharedMemoryEntry types + zod schemas"
      exports: ["workspaceFormSchema", "CollaborationMode", "MemoryStrategy"]
    - path: "packages/web/lib/workspace/actions.ts"
      provides: "Server actions for workspace CRUD + agent assignment"
      exports: ["createWorkspace", "updateWorkspace", "deleteWorkspace", "addAgentToWorkspace", "removeAgentFromWorkspace"]
    - path: "packages/web/lib/workspace/queries.ts"
      provides: "Server queries for workspace data"
      exports: ["getWorkspaces", "getWorkspaceById", "getWorkspaceAgents"]
    - path: "packages/web/lib/workspace/shared-memory.ts"
      provides: "Shared memory read/write/upsert"
      exports: ["readSharedMemory", "writeSharedMemory", "upsertSharedMemory"]
  key_links:
    - from: "packages/web/lib/workspace/actions.ts"
      to: "supabase"
      via: "server client with user session"
      pattern: "createSupabaseServerClient"
    - from: "packages/web/lib/workspace/shared-memory.ts"
      to: "shared_memory table"
      via: "upsert with ON CONFLICT"
      pattern: "supabase.*shared_memory"
---

<objective>
Create the database migration for workspace_agents join table and implement all workspace server-side logic: types, CRUD actions, queries, shared memory operations, and agent assignment.

Purpose: Establishes the data layer foundation that all other Phase 5 plans depend on -- orchestrator, UI, and real-time features all consume these types and actions.
Output: Migration SQL, TypeScript types with zod schemas, server actions, server queries, shared memory utilities, and passing tests for all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-agent/05-RESEARCH.md

@packages/web/lib/agent/types.ts
@packages/web/lib/agent/actions.ts
@packages/web/lib/agent/queries.ts
@supabase/migrations/20260412004330_initial_schema.sql

<interfaces>
<!-- Existing workspace schema (from initial_schema.sql): -->
<!-- workspaces(id, tenant_id, name, description, collaboration_mode, conflict_resolution, created_by, created_at, updated_at) -->
<!-- shared_memory(id, tenant_id, workspace_id, key, value, updated_by, created_at, updated_at) -->
<!-- collaboration_mode CHECK: ('round_robin', 'hierarchical', 'democratic', 'parallel') -->
<!-- conflict_resolution CHECK: ('vote', 'hierarchy', 'consensus') -->
<!-- RLS policies already exist for workspaces and shared_memory tables -->

From packages/web/lib/auth/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>
```

From packages/web/lib/agent/types.ts:
```typescript
export const agentFormSchema = z.object({ ... })
export type AgentFormData = z.infer<typeof agentFormSchema>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration + workspace types and zod schemas</name>
  <files>
    supabase/migrations/20260412200000_phase5_workspace_agents.sql,
    packages/web/lib/workspace/types.ts,
    packages/web/lib/workspace/__tests__/types.test.ts
  </files>
  <read_first>
    supabase/migrations/20260412004330_initial_schema.sql,
    packages/web/lib/agent/types.ts,
    packages/shared/types/database.ts
  </read_first>
  <behavior>
    - Test: workspaceFormSchema validates correct input (name, description, collaboration_mode, memory_strategy, conflict_resolution, agent_ids)
    - Test: workspaceFormSchema rejects empty name
    - Test: workspaceFormSchema rejects invalid collaboration_mode
    - Test: workspaceFormSchema rejects invalid memory_strategy
    - Test: CollaborationMode type includes round_robin, parallel, sequential, hierarchical, democratic
    - Test: MemoryStrategy type includes conversation, summary, vector, hybrid
  </behavior>
  <action>
    1. Create migration `supabase/migrations/20260412200000_phase5_workspace_agents.sql`:
       - CREATE TABLE workspace_agents (id uuid PK, tenant_id uuid NOT NULL FK tenants, workspace_id uuid NOT NULL FK workspaces, agent_id uuid NOT NULL FK agents, role text NOT NULL DEFAULT 'member' CHECK (role IN ('leader', 'member')), position integer NOT NULL DEFAULT 0, created_at timestamptz NOT NULL DEFAULT now(), UNIQUE(workspace_id, agent_id))
       - ALTER TABLE workspaces ADD COLUMN memory_strategy text NOT NULL DEFAULT 'conversation' CHECK (memory_strategy IN ('conversation', 'summary', 'vector', 'hybrid'))
       - ALTER TABLE workspaces ADD COLUMN compression_enabled boolean NOT NULL DEFAULT true
       - ALTER TABLE workspaces ADD COLUMN compression_threshold integer NOT NULL DEFAULT 80
       - ALTER TABLE workspaces DROP CONSTRAINT IF EXISTS workspaces_collaboration_mode_check, then ADD CONSTRAINT to include 'sequential': CHECK (collaboration_mode IN ('round_robin', 'hierarchical', 'democratic', 'parallel', 'sequential'))
       - ENABLE RLS on workspace_agents
       - CREATE POLICY workspace_agents_select FOR SELECT using tenant_id = get_current_tenant_id()
       - CREATE POLICY workspace_agents_insert FOR INSERT with check tenant_id = get_current_tenant_id()
       - CREATE POLICY workspace_agents_update FOR UPDATE using/with check tenant_id = get_current_tenant_id()
       - CREATE POLICY workspace_agents_delete FOR DELETE using tenant_id = get_current_tenant_id()
       - CREATE INDEX idx_workspace_agents_workspace ON workspace_agents(workspace_id)
       - CREATE INDEX idx_workspace_agents_tenant ON workspace_agents(tenant_id)

    2. Create `packages/web/lib/workspace/types.ts`:
       - Export const COLLABORATION_MODES = ['round_robin', 'parallel', 'sequential', 'hierarchical', 'democratic'] as const
       - Export type CollaborationMode = typeof COLLABORATION_MODES[number]
       - Export const MEMORY_STRATEGIES = ['conversation', 'summary', 'vector', 'hybrid'] as const
       - Export type MemoryStrategy = typeof MEMORY_STRATEGIES[number]
       - Export const CONFLICT_RESOLUTIONS = ['vote', 'hierarchy', 'consensus'] as const
       - Export type ConflictResolution = typeof CONFLICT_RESOLUTIONS[number]
       - Export const AGENT_ROLES = ['leader', 'member'] as const
       - Export type AgentRole = typeof AGENT_ROLES[number]
       - Export interface Workspace { id, tenant_id, name, description, collaboration_mode, conflict_resolution, memory_strategy, compression_enabled, compression_threshold, created_by, created_at, updated_at }
       - Export interface WorkspaceAgent { id, tenant_id, workspace_id, agent_id, role, position, created_at }
       - Export interface SharedMemoryEntry { id, tenant_id, workspace_id, key, value (unknown), updated_by, created_at, updated_at }
       - Export const workspaceFormSchema = z.object({ name: z.string().min(1).max(100), description: z.string().max(500).optional(), collaboration_mode: z.enum(COLLABORATION_MODES), memory_strategy: z.enum(MEMORY_STRATEGIES).default('conversation'), conflict_resolution: z.enum(CONFLICT_RESOLUTIONS).default('vote'), compression_enabled: z.boolean().default(true), compression_threshold: z.number().int().min(0).max(100).default(80), agent_ids: z.array(z.string().uuid()).default([]) })
       - Export type WorkspaceFormData = z.infer<typeof workspaceFormSchema>

    3. Write tests in __tests__/types.test.ts for all schema validations listed in <behavior>.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run lib/workspace/__tests__/types.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at supabase/migrations/20260412200000_phase5_workspace_agents.sql
    - grep "create table workspace_agents" returns match in migration file
    - grep "memory_strategy" returns match in migration file
    - grep "sequential" returns match in migration file (collaboration_mode check updated)
    - grep "workspaceFormSchema" packages/web/lib/workspace/types.ts returns match
    - All 6 test assertions pass
  </acceptance_criteria>
  <done>Migration file ready for push, types and schemas exported, all schema validation tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workspace CRUD actions + queries + shared memory + tests</name>
  <files>
    packages/web/lib/workspace/actions.ts,
    packages/web/lib/workspace/queries.ts,
    packages/web/lib/workspace/shared-memory.ts,
    packages/web/lib/workspace/__tests__/actions.test.ts,
    packages/web/lib/workspace/__tests__/queries.test.ts,
    packages/web/lib/workspace/__tests__/shared-memory.test.ts
  </files>
  <read_first>
    packages/web/lib/agent/actions.ts,
    packages/web/lib/agent/queries.ts,
    packages/web/lib/auth/server.ts,
    packages/web/lib/workspace/types.ts
  </read_first>
  <behavior>
    - Test: createWorkspace calls supabase insert on workspaces table and returns workspace
    - Test: createWorkspace inserts workspace_agents records for provided agent_ids
    - Test: updateWorkspace calls supabase update with correct fields
    - Test: deleteWorkspace calls supabase delete
    - Test: addAgentToWorkspace inserts into workspace_agents
    - Test: removeAgentFromWorkspace deletes from workspace_agents
    - Test: getWorkspaces returns array of workspaces for tenant
    - Test: getWorkspaceById returns workspace with agents joined
    - Test: getWorkspaceAgents returns agents for workspace
    - Test: readSharedMemory returns all entries for workspace
    - Test: writeSharedMemory inserts new entry
    - Test: upsertSharedMemory uses ON CONFLICT UPDATE for existing key
  </behavior>
  <action>
    1. Write ALL test files FIRST (TDD red phase). Mock supabase client using vi.mock or manual mock factory that returns chainable .from().select().eq() etc. Each test asserts the correct supabase method chain was called with correct args.

    2. Create `packages/web/lib/workspace/actions.ts`:
       - 'use server' directive at top
       - Import createSupabaseServerClient from @/lib/auth/server
       - Import workspaceFormSchema, WorkspaceFormData from ./types
       - Export async function createWorkspace(data: WorkspaceFormData): creates workspace row, then bulk-inserts workspace_agents for each agent_id. Uses supabase transaction pattern (insert workspace, get id, insert workspace_agents). Returns { data: workspace, error: null } or { data: null, error: string }.
       - Export async function updateWorkspace(id: string, data: Partial<WorkspaceFormData>): updates workspace fields. If agent_ids provided, delete existing workspace_agents and re-insert.
       - Export async function deleteWorkspace(id: string): deletes workspace (cascade handles workspace_agents via FK).
       - Export async function addAgentToWorkspace(workspaceId: string, agentId: string, role?: AgentRole, position?: number): inserts single workspace_agent row. Requires tenantId from workspace lookup.
       - Export async function removeAgentFromWorkspace(workspaceId: string, agentId: string): deletes workspace_agent row.

    3. Create `packages/web/lib/workspace/queries.ts`:
       - Import createSupabaseServerClient from @/lib/auth/server
       - Export async function getWorkspaces(): select from workspaces, order by created_at desc. RLS handles tenant scoping.
       - Export async function getWorkspaceById(id: string): select from workspaces where id, plus select from workspace_agents where workspace_id joined with agents table to get agent details.
       - Export async function getWorkspaceAgents(workspaceId: string): select from workspace_agents where workspace_id, join agents, order by position asc.

    4. Create `packages/web/lib/workspace/shared-memory.ts`:
       - Import createSupabaseServerClient
       - Export async function readSharedMemory(workspaceId: string): select from shared_memory where workspace_id, order by updated_at desc.
       - Export async function writeSharedMemory(workspaceId: string, key: string, value: unknown, agentId?: string): insert into shared_memory. Lookup workspace to get tenant_id.
       - Export async function upsertSharedMemory(workspaceId: string, key: string, value: unknown, agentId?: string): use supabase .upsert() with onConflict: 'workspace_id,key' to handle existing keys.
       - Cap value size: validate JSON.stringify(value).length <= 65536 (64KB per key) before write. Throw error if exceeded.

    5. Run tests (TDD green phase) -- all tests should now pass.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run lib/workspace/__tests__/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - grep "createWorkspace" packages/web/lib/workspace/actions.ts returns match
    - grep "getWorkspaceById" packages/web/lib/workspace/queries.ts returns match
    - grep "upsertSharedMemory" packages/web/lib/workspace/shared-memory.ts returns match
    - grep "65536" packages/web/lib/workspace/shared-memory.ts returns match (64KB cap)
    - All test files in lib/workspace/__tests__/ pass
  </acceptance_criteria>
  <done>Workspace CRUD, agent assignment, shared memory all implemented with full test coverage. All actions use server client (never service role). 64KB shared memory value cap enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client to API | User input for workspace creation/update crosses trust boundary |
| API to Database | Server actions query Supabase with user session (RLS enforced) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | workspace form input | mitigate | Validate all input with zod workspaceFormSchema before any DB operation |
| T-05-02 | Information Disclosure | cross-tenant workspace access | mitigate | RLS policies on workspace_agents table; all queries use user session client |
| T-05-03 | Elevation of Privilege | shared_memory value injection | mitigate | Cap value size at 64KB; validate with zod before writing |
| T-05-04 | Tampering | workspace_agents role escalation | mitigate | role CHECK constraint in DB ('leader', 'member'); validated server-side |
</threat_model>

<verification>
1. All test files in packages/web/lib/workspace/__tests__/ pass
2. Migration file contains workspace_agents table, RLS, indexes
3. Types export all required interfaces and schemas
4. Actions use createSupabaseServerClient (never service role)
</verification>

<success_criteria>
- Migration ready for `supabase db push`
- Workspace CRUD: create, read, update, delete all tested
- Agent assignment: add/remove agents to/from workspaces tested
- Shared memory: read/write/upsert with 64KB cap tested
- All tests green: `cd packages/web && npx vitest run lib/workspace/__tests__/`
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-agent/05-01-SUMMARY.md`
</output>
