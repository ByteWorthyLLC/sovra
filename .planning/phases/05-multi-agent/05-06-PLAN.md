---
phase: 05-multi-agent
plan: 06
type: execute
wave: 3
depends_on: ["05-04", "05-05"]
files_modified:
  - packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx
  - packages/web/components/workspace/workspace-detail-header.tsx
  - packages/web/components/workspace/activity-feed.tsx
  - packages/web/components/workspace/agent-panel.tsx
  - packages/web/components/workspace/shared-memory-panel.tsx
  - packages/web/components/workspace/workspace-settings-sheet.tsx
  - packages/web/components/workspace/socket-status-indicator.tsx
  - packages/web/components/workspace/context-compression-indicator.tsx
  - packages/web/components/workspace/conflict-resolution-card.tsx
  - packages/web/app/globals.css
autonomous: true
requirements:
  - MUL-04
  - MUL-05
  - MUL-06
  - REAL-03
  - REAL-04
  - REAL-05

must_haves:
  truths:
    - "Workspace detail page shows activity feed, agent panel, and shared memory panel"
    - "Agent status updates appear in real-time via Socket.IO"
    - "Agent-to-agent messages appear in the activity feed"
    - "Shared memory panel shows current workspace memory entries"
    - "Settings sheet allows editing collaboration mode, memory strategy, and agent assignment"
    - "Socket status indicator shows connected/reconnecting/disconnected state"
    - "Conflict resolution card appears inline in activity feed when agents disagree"
  artifacts:
    - path: "packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx"
      provides: "Workspace detail page"
    - path: "packages/web/components/workspace/activity-feed.tsx"
      provides: "Activity feed with auto-scroll and streaming support"
    - path: "packages/web/components/workspace/agent-panel.tsx"
      provides: "Right panel showing workspace agents with live status"
    - path: "packages/web/components/workspace/shared-memory-panel.tsx"
      provides: "Collapsible bottom panel for shared memory entries"
    - path: "packages/web/components/workspace/workspace-settings-sheet.tsx"
      provides: "Settings sheet with General, Collaboration, Memory tabs"
    - path: "packages/web/components/workspace/socket-status-indicator.tsx"
      provides: "Connection status dot + label"
    - path: "packages/web/components/workspace/conflict-resolution-card.tsx"
      provides: "Inline conflict resolution panel in activity feed"
  key_links:
    - from: "packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx"
      to: "packages/web/lib/realtime/use-workspace-socket.ts"
      via: "useWorkspaceSocket hook in client wrapper"
      pattern: "useWorkspaceSocket"
    - from: "packages/web/components/workspace/activity-feed.tsx"
      to: "packages/web/lib/realtime/workspace-store.ts"
      via: "useWorkspaceStore for activity events"
      pattern: "useWorkspaceStore"
    - from: "packages/web/components/workspace/agent-panel.tsx"
      to: "packages/web/lib/realtime/workspace-store.ts"
      via: "useWorkspaceStore for agent statuses"
      pattern: "agentStatuses"
---

<objective>
Build the workspace detail page -- the centerpiece of multi-agent collaboration. Includes the activity feed showing live agent messages, agent panel with real-time status, shared memory panel, settings sheet, socket status indicator, context compression indicator, and conflict resolution card.

Purpose: This is where users observe and control multi-agent collaboration. Every real-time event (agent status, messages, streaming chunks, conflicts) surfaces here through the Socket.IO hook and zustand store built in Plan 05.
Output: Workspace detail page with all sub-components, fully wired to real-time infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-agent/05-UI-SPEC.md
@.planning/phases/05-multi-agent/05-04-SUMMARY.md
@.planning/phases/05-multi-agent/05-05-SUMMARY.md

@packages/web/lib/realtime/use-workspace-socket.ts
@packages/web/lib/realtime/workspace-store.ts
@packages/web/lib/workspace/types.ts
@packages/web/lib/workspace/queries.ts
@packages/web/lib/workspace/actions.ts
@packages/web/lib/workspace/shared-memory.ts
@packages/web/lib/motion.ts

<interfaces>
From packages/web/lib/realtime/workspace-store.ts (Plan 05):
```typescript
export const useWorkspaceStore = create<WorkspaceStoreState>(...)
// State: connectionStatus, agentStatuses (Map), activityFeed (ActivityEvent[]), streamingChunks (Map)
// Actions: setConnectionStatus, updateAgentStatus, addActivity, appendChunk, clearChunks, reset
```

From packages/web/lib/realtime/use-workspace-socket.ts (Plan 05):
```typescript
export function useWorkspaceSocket(tenantId: string, workspaceId: string): RefObject<Socket | null>
```

From packages/web/lib/workspace/queries.ts (Plan 01):
```typescript
export async function getWorkspaceById(id: string)
export async function getWorkspaceAgents(workspaceId: string)
```

From packages/web/lib/workspace/shared-memory.ts (Plan 01):
```typescript
export async function readSharedMemory(workspaceId: string)
```

From packages/web/lib/workspace/actions.ts (Plan 01):
```typescript
export async function updateWorkspace(id: string, data: Partial<WorkspaceFormData>)
export async function addAgentToWorkspace(workspaceId, agentId, role?, position?)
export async function removeAgentFromWorkspace(workspaceId, agentId)
```

From packages/web/lib/motion.ts:
```typescript
export const VARIANTS = { messageEnter, slideInRight, conflictEnter, jumpToLatest, ... }
export const TRANSITIONS = { spring, springFast, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace detail page + header + activity feed + agent panel</name>
  <files>
    packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx,
    packages/web/components/workspace/workspace-detail-header.tsx,
    packages/web/components/workspace/activity-feed.tsx,
    packages/web/components/workspace/agent-panel.tsx,
    packages/web/components/workspace/socket-status-indicator.tsx,
    packages/web/app/globals.css
  </files>
  <read_first>
    .planning/phases/05-multi-agent/05-UI-SPEC.md,
    packages/web/lib/realtime/use-workspace-socket.ts,
    packages/web/lib/realtime/workspace-store.ts,
    packages/web/lib/workspace/queries.ts,
    packages/web/app/(tenant)/t/[slug]/agents/page.tsx,
    packages/web/app/globals.css,
    packages/web/lib/motion.ts
  </read_first>
  <action>
    1. Add CSS keyframes to `packages/web/app/globals.css` (append, do not modify existing):
       ```css
       @keyframes statusFlash {
         0% { border-color: rgba(59, 130, 246, 0.8); }
         100% { border-color: var(--border); }
       }
       .status-flash { animation: statusFlash 0.6s ease-out; }
       ```
       Note: statusPulse keyframe should already exist from Phase 3. If not, add:
       ```css
       @keyframes statusPulse {
         0%, 100% { opacity: 1; }
         50% { opacity: 0.4; }
       }
       .status-pulse { animation: statusPulse 1.2s ease-in-out infinite; }
       ```

    2. Create `packages/web/components/workspace/socket-status-indicator.tsx`:
       - 'use client'
       - Import useWorkspaceStore
       - Render connection dot + label per UI spec:
         - connected: green dot, "Live" label (hidden on narrow viewports)
         - reconnecting: amber dot with status-pulse animation, "Reconnecting..."
         - disconnected: red dot, "Offline"
       - aria-label="Connection status: {state}"

    3. Create `packages/web/components/workspace/workspace-detail-header.tsx`:
       - Props: workspace (server data), onSettingsOpen callback
       - Back button (ArrowLeft icon, links to /workspaces list)
       - Workspace name (text-2xl font-semibold tracking-tight)
       - Collaboration mode badge (same color scheme as workspace-card)
       - Settings button (Settings icon, aria-label="Workspace settings")
       - SocketStatusIndicator (right side)
       - "Start collaboration" primary button (right side, triggers /api/workspaces/[id]/run)
       - h-14 border-b border-border, sticky

    4. Create `packages/web/components/workspace/activity-feed.tsx`:
       - 'use client'
       - Import useWorkspaceStore for activityFeed and streamingChunks
       - overflow-y-auto, px-6 py-4
       - Map activityFeed to ActivityFeedItem components:
         - system_event: Info icon (14px muted) + text (text-sm text-muted-foreground)
         - agent_message: Agent name chip + arrow + target agent name + message content (text-sm leading-6) + timestamp
         - conflict: render ConflictResolutionCard component
       - Streaming state: show accumulated chunks for currently streaming agents with blinking cursor (streaming-cursor class from Phase 3)
       - Auto-scroll behavior: auto-scroll to bottom on new event. If user scrolled up, show "View latest" pill (jumpToLatest variant, AnimatePresence)
       - Empty state: centered "No activity yet. Start collaboration to see agent messages here."
       - Each item enters with messageEnter variant

    5. Create `packages/web/components/workspace/agent-panel.tsx`:
       - 'use client'
       - 320px right panel, border-l border-border
       - "Agents" label (text-sm font-semibold, px-4 py-3, border-b)
       - Map workspace agents to compact cards (h-[72px]):
         - Left: status dot (6px, color by agent status from useWorkspaceStore.agentStatuses) + agent name (text-sm font-semibold)
         - Below name: model info (text-xs text-muted-foreground)
         - Right: remove button (X icon, 24px, appears on hover)
         - Active state (currently running): border-left 2px solid blue-500, status dot pulsing
         - Status flash: CSS statusFlash keyframe on real-time status change
       - "+ Add agent" button at bottom
       - "Start collaboration" primary button (w-full, bottom, p-4)
       - On "Start collaboration" click: POST to /api/workspaces/[id]/run with empty prompt (or show prompt input)

    6. Create workspace detail page `packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx`:
       - Server component that fetches workspace data, then renders a client wrapper
       - Server: getWorkspaceById(workspaceId), getWorkspaceAgents(workspaceId), readSharedMemory(workspaceId)
       - If workspace not found, return notFound()
       - Client wrapper component (separate 'use client' component or inline):
         - Calls useWorkspaceSocket(tenantId, workspaceId) to connect
         - Renders: WorkspaceDetailHeader + main canvas (flex row)
         - Main canvas: ActivityFeed (flex-1) + SharedMemoryPanel (bottom collapsible) + AgentPanel (320px right)
         - Mobile: AgentPanel hidden behind sheet, triggered by icon button in header
       - Load animation: stagger 0.06s header -> feed -> agent panel
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/__tests__/workspace/ --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: packages/web/src/__tests__/workspace/workspace-detail.test.ts (store interaction tests for MUL-04, REAL-03)
    - File exists: packages/web/app/(tenant)/t/[slug]/workspaces/[workspaceId]/page.tsx
    - grep "useWorkspaceSocket" in the [workspaceId] page or its client wrapper returns match
    - grep "useWorkspaceStore" packages/web/components/workspace/activity-feed.tsx returns match
    - grep "agentStatuses" packages/web/components/workspace/agent-panel.tsx returns match
    - grep "statusFlash" packages/web/app/globals.css returns match
    - grep for all 3 socket states in socket-status-indicator.tsx returns matches
    - grep "Start collaboration" packages/web/components/workspace/agent-panel.tsx returns match
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Workspace detail page renders with header, activity feed, agent panel, and socket connection. Real-time events from zustand store drive agent status dots, activity feed items, and streaming chunks. Mobile responsive with agent panel in sheet.</done>
</task>

<task type="auto">
  <name>Task 2: Shared memory panel + settings sheet + conflict card + compression indicator</name>
  <files>
    packages/web/components/workspace/shared-memory-panel.tsx,
    packages/web/components/workspace/workspace-settings-sheet.tsx,
    packages/web/components/workspace/conflict-resolution-card.tsx,
    packages/web/components/workspace/context-compression-indicator.tsx
  </files>
  <read_first>
    .planning/phases/05-multi-agent/05-UI-SPEC.md,
    packages/web/lib/workspace/types.ts,
    packages/web/lib/workspace/actions.ts,
    packages/web/lib/workspace/shared-memory.ts,
    packages/web/lib/motion.ts
  </read_first>
  <action>
    1. Create `packages/web/components/workspace/context-compression-indicator.tsx`:
       - 'use client'
       - Props: usedTokens: number, maxTokens: number, compressionEnabled: boolean
       - Only visible when compression enabled and usage >= 50%
       - Token count display: text-sm font-mono "{N}K / {M}K tokens"
       - Progress bar: w-[80px] h-1 rounded-full bg-zinc-800
         - Fill: transition-[width] duration-400 ease-out
         - Color: <60% bg-blue-500, 60-80% bg-amber-400, >80% bg-red-500
       - Warning state (>80%): token count text-amber-400, tooltip "Context compression active. Older messages condensed to stay within token limits."

    2. Create `packages/web/components/workspace/shared-memory-panel.tsx`:
       - 'use client'
       - Collapsible bottom panel per UI spec
       - min-h-[200px] default, max-h-[400px], border-t border-border bg-card/50 backdrop-blur-sm
       - Header: "Shared Memory" label + memory strategy tag (colored per spec) + ContextCompressionIndicator (right-aligned)
       - Collapse toggle: ChevronDown icon, aria-label="Collapse shared memory" / "Expand shared memory"
       - Framer Motion height animation (animate: { height: 0 / "auto" }), overflow-hidden
       - Memory entries list (scrollable):
         - Each entry: agent name (text-xs font-semibold muted-foreground) + strategy tag + timestamp (right)
         - Content: text-sm, max 2 lines truncated with "Show more" expand toggle (text-xs text-primary)
       - Empty state: "Shared memory is empty. It fills as agents collaborate."

    3. Create `packages/web/components/workspace/conflict-resolution-card.tsx`:
       - 'use client'
       - Renders inline within activity feed when conflict event appears
       - conflictEnter animation variant
       - Structure per UI spec:
         - "Conflict resolution" header (text-xs font-semibold uppercase, amber)
         - Proposal A: [Agent name] + truncated proposal text
         - Proposal B: [Agent name] + truncated proposal text
         - Vote tally: [A: N] [B: N] [Pending: N] badges
         - Status: PENDING (amber) or RESOLVED (green)
         - [View full proposals] expand toggle
       - Resolved state: bg shifts to rgba(34,197,94,0.05), border rgba(34,197,94,0.2)

    4. Create `packages/web/components/workspace/workspace-settings-sheet.tsx`:
       - 'use client'
       - Slides in from right (slideInRight variant), width 480px desktop / full-screen mobile
       - Sheet header: "Workspace settings" (text-xl font-semibold) + close button
       - 3 tabs: General | Collaboration | Memory (use Radix Tabs)
       
       Tab: General
         - Workspace name input (required, autofocused)
         - Description textarea (optional, 3 rows)
         - Agent assignment multi-select list with checkboxes
           - Each row: agent name + model chip + checkbox
           - "No agents available" empty state

       Tab: Collaboration
         - Collaboration mode select with descriptions per copywriting contract:
           - round_robin: "Each agent responds in turn, cycling through all assigned agents."
           - parallel: "All agents respond simultaneously to the same input."
           - sequential: "Agents respond in a fixed order you define."
           - hierarchical: "A lead agent delegates tasks to sub-agents as needed."
           - democratic: "Agents vote to determine the best response when they disagree."
         - Conflict resolution select (shown when mode is democratic or parallel): vote / hierarchy / consensus
         - Max concurrent agents (number 1-20, shown for parallel mode)

       Tab: Memory
         - Memory strategy select with descriptions per copywriting contract
         - Context compression toggle (default: on)
         - Compression threshold (0-100, default: 80, shown when compression on)
         - Helper text: "Context compresses when N% of the model's token limit is used"

       Footer (sticky): [Cancel] ghost + [Save settings] primary, right-aligned
       - "Unsaved changes" warning (text-xs text-amber-400, left) when form dirty
       - On save: call updateWorkspace server action, close sheet

       Tab switch animation: content fade + translateX +/-8px, 150ms
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/__tests__/workspace/ --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "Shared Memory" packages/web/components/workspace/shared-memory-panel.tsx returns match
    - grep "conflictEnter" packages/web/components/workspace/conflict-resolution-card.tsx returns match
    - grep "Workspace settings" packages/web/components/workspace/workspace-settings-sheet.tsx returns match
    - grep "memory_strategy" packages/web/components/workspace/workspace-settings-sheet.tsx returns match
    - grep "compression_threshold" packages/web/components/workspace/workspace-settings-sheet.tsx returns match
    - grep "Unsaved changes" packages/web/components/workspace/workspace-settings-sheet.tsx returns match
    - No em dashes (U+2014) in any created file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Shared memory panel with collapse animation and compression indicator. Settings sheet with 3 tabs covering all workspace configuration. Conflict resolution card for inline display in activity feed. All copy matches UI spec, no em dashes, all TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User to settings sheet | User modifies workspace configuration |
| Socket.IO events to UI | Real-time data rendered in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Tampering | workspace settings form | mitigate | zod validation on all form fields before server action call. Server action re-validates. |
| T-05-20 | Information Disclosure | shared memory panel | mitigate | readSharedMemory uses Supabase client with user session. RLS prevents cross-tenant reads. |
| T-05-21 | Tampering | XSS via agent message content | mitigate | React auto-escapes all JSX text content. No raw HTML injection used in any component. Agent messages rendered as plain text nodes only. |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. Workspace detail page renders all panels
3. Settings sheet has 3 tabs with all configuration options
4. Conflict resolution card uses correct animations
5. No em dashes in any component copy
</verification>

<success_criteria>
- Workspace detail page: header, activity feed, agent panel, shared memory panel
- Activity feed: auto-scroll, streaming chunks, "View latest" pill
- Agent panel: live status dots, status flash, start collaboration button
- Shared memory: collapsible, compression indicator, strategy tags
- Settings sheet: General/Collaboration/Memory tabs, unsaved changes warning
- Conflict card: inline in feed, vote tally, resolved state
- All TypeScript compiles, no em dashes
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-agent/05-06-SUMMARY.md`
</output>
