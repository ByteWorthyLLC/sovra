name: Fuzz

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 9 * * 2"

concurrency:
  group: fuzz-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  worker-go-fuzz:
    name: Worker Go Fuzz
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16
        with:
          go-version-file: packages/worker/go.mod
          cache-dependency-path: packages/worker/go.sum

      - name: Run fuzz targets (bounded)
        working-directory: packages/worker
        run: |
          go test ./internal/config -run=^$ -fuzz=^FuzzIsProduction$ -fuzztime=10s
          go test ./internal/config -run=^$ -fuzz=^FuzzValidateAuthConfig$ -fuzztime=10s
